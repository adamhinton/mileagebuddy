// README
// This is to make sure that reality is still real.
// If this test fails, you probably have bigger things to worry about than this project.

describe("Sanity check", () => {
	it("2 + 2 = 4", () => {
		const twoPlusTwo = 2 + 2;
		expect(twoPlusTwo).toBe(4);
	});
});
