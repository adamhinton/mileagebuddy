import type { Config } from "tailwindcss";

// Colors and dark mode are configured via CSS (@theme / @variant dark) in
// src/app/globals.css and src/app/styles/themes.css.
export default {} satisfies Config;
